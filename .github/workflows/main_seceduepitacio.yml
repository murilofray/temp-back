# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - seceduepitacio

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://index.docker.io/v1/
        username: ${{ secrets.AzureAppService_ContainerUsername_ed7a125bf6624a05b7fe0e33389ab067 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_fd7908c1303f4b8095ec986739418700 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: index.docker.io/${{ secrets.AzureAppService_ContainerUsername_ed7a125bf6624a05b7fe0e33389ab067 }}/offmurilo/sec:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'seceduepitacio'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_ca1ab50f370e43fc88d11efd956d22e3 }}
        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_ed7a125bf6624a05b7fe0e33389ab067 }}/offmurilo/sec:${{ github.sha }}'